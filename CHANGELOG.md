# Changelog

All notable changes to this project will be documented in this file.

## [v1.0.15] - 2026-06-15
### Added
- **引入纯净无感的「晨间自动热更新」守护机制**：彻底解决 Hosts 文件长期不更新导致节点失效的问题。基于 Inode 永固覆盖写入技术与 GMS 强制断链热重载机制，模块现已实现在**完全无须重启手机、无须弹窗打扰用户**的情况下进行静默更新。模块每天会在后台以 30 分钟的极低频周期嗅探时间，只要时间超过早晨 07:00 且当日未更新，即会自动拉取最新优选节点、热重载 GMS 并留下日期防抖印记（随后深度休眠 12 小时），保证你每天醒来拿到的都是最新鲜的低延迟 FCM 通道。
- **引入业界标准的「弹性指数退避算法 (Exponential Backoff Polling)」**：针对底层强轮询架构可能引发的耗电问题，彻底重构了网络状态睡眠机制。模块现在具备动态的自适应嗅探频率：在网络状态静默时，轮询间隔会以 `3s -> 6s -> 12s -> 24s -> 30s` 的指数级曲线逐步拉长，确保设备能完美进入 Android Doze 深度休眠，杜绝无意义的 CPU 唤醒耗电；而一旦侦测到任何网络变动（如切换 Wi-Fi 或开启透明代理），探测器会瞬间将休眠重置为 3 秒的最高灵敏度频率。完美平衡了“零漏触秒级响应”与“极限省电”这一对底层矛盾，达到工业级工程优化水准。

### Fixed
- **修正长期未命中同一个非优选 IP 的逻辑缺失 Bug**：查明当设备切换网络（如开关透明代理或切换 Wi-Fi）时，GMS 会立刻发起抢跑式重连并长期保持持久化套接字不断开。旧版本在检测到网络切换后，仅仅被动地“清理防火墙和更新 Hosts”，并未主动切断 GMS 抢跑产生的非法长连接。现已在网络监视器判定网卡切换后，新增 `killall -9` 强杀 GMS 常驻进程的惩罚性断链机制，迫使 FCM 立刻重置网络并严丝合缝地应用新挂载的优选 Hosts。
- **废弃失效的事件管道驱动，全面切入强状态比对架构**：先前的版本受困于 `ip monitor` 的 Linux Block Buffering（管道 4KB 块缓冲死锁）以及大量国产定制系统在底层屏蔽 `ConnectivityService` 日志流的限制，导致面对飞行模式秒切、代理高频开闭时直接发生死寂停摆。现已完全抛弃不可靠的 Shell 事件驱动，100% 切换为更稳健的底层物理网卡出口暴力比对模式。再无漏监控的死角！
- **UI 文本优化**：移除了安装过程引导提示语句中的冗余字眼。

## [v1.0.14] - 2026-06-12
### Added
- **引入业界标准的「弹性指数退避算法 (Exponential Backoff Polling)」**：彻底重构了网络状态睡眠机制。模块现在具备动态的自适应嗅探频率：在网络状态静默时，轮询间隔会以 `3s -> 6s -> 12s -> 24s -> 30s` 的指数级曲线逐步拉长，确保设备能完美进入 Android Doze 深度休眠，杜绝无意义的 CPU 唤醒耗电；而一旦侦测到任何网络变动（如切换 Wi-Fi 或开启透明代理），探测器会瞬间将休眠重置为 3 秒的最高灵敏度频率。完美兼顾了“零漏触的秒级响应”与“极限省电”，达到工业级工程优化水准。

### Fixed
- **修正长期未命中同一个非优选 IP 的逻辑缺失 Bug**：查明当设备切换网络（如开关透明代理或切换 Wi-Fi）时，GMS 会立刻发起抢跑式重连并长期保持持久化套接字不断开。旧版本在检测到网络切换后，仅仅被动地“清理防火墙和更新 Hosts”，并未主动切断 GMS 抢跑产生的非法长连接，导致日志永远报告未命中同一个非优选 IP。现已在网络监视器判定网卡切换后，新增 `killall -9` 强杀 GMS 常驻进程的惩罚性断链机制，迫使 FCM 立刻重置网络并严丝合缝地读取应用新挂载的优选 Hosts。
- **废弃失效的事件管道驱动，全面切入强状态比对架构**：先前的版本受困于 `ip monitor` 等管道输出会被系统底层的 4KB Linux Block Buffering（块缓冲区）死死卡住的缺陷，导致面对飞行模式秒切、透明代理工具高频启动时直接发生“死寂停摆”。现已完全抛弃不可靠的 Shell 事件驱动，100% 切换为更稳健的底层物理网卡出口暴力核对模式。再无漏监控的死角！
- **UI 文本优化**：修正了模块安装引导提示语句中的冗余字眼。

## [v1.0.13] - 2026-06-12
### Changed
- **重构为「零耗电 Logcat 事件驱动架构」**：抛弃了基于 `ip monitor` 会受困于 4KB Linux Block Buffering（管道缓冲区死锁）的问题，同时直接废弃了会打破 Android Doze 深度休眠机制的高耗电 `sleep` 轮询脏方案。新架构通过挂起监听 Android 原生的 `ConnectivityService` 系统流，实现了对基带唤醒、飞行模式秒切、代理客户端开闭的 **毫秒级 0 耗电** 绝对响应，达成工业级网络守护标准。

### Fixed
- **完美穿透透明代理 TUN 网卡探测漏洞**：将网络连通性测探与真实物理网卡探测的目标 IP 由海外的 `8.8.8.8` 替换为国内公网的 `223.5.5.5`。彻底解决了当设备运行 Clash / Box 等透明代理时，系统底层路由查询被强制接管并永远错误返回虚拟网卡 `dev tun0` 的现象，使模块能精准识别当前的物理底层链路出口。

## [v1.0.12] - 2026-06-12
### Fixed
- **重构事件监视器为「无损异步哨兵模型」，彻底根除吞事件与慢网络漏触**：上一版本的线性防抖逻辑在面对 5G/4G 慢速基带拨号（需长达数秒才能获取 IP）或极高频的网络翻转（秒切飞行模式/开关透明代理）时，会导致大量合法的底层默认路由下发事件落在冷却期死角中被无情吞噬。现已将监视器拆分为极度轻量的“前端信号哨兵”与“后端轮询工人”队列模型。后端不仅加入了长达 15 秒的弹性网络连通性等候机制，还移除了错误的排他期信号销毁指令。完美实现了期间事件堆叠防抖、期后立即二次响应的能力，百发百中！
- **完美穿透透明代理 TUN 劫持探测**：将网络连通性测探与真实物理网卡探测的目标 IP 由海外的 `8.8.8.8` 替换为国内公网 `223.5.5.5`。彻底解决了当设备运行 Box for Root 或 FlClash 时，路由查询被强制接管并永远返回虚拟网卡 `dev tun0` 的尴尬问题，使日志能精准定位当前的真实底层出口（如 `wlan0`, `rmnet_data0`）。
- **彻底覆盖全维内核事件盲区**：扩大了 `ip monitor` 的监听维面（由单一的 `route` 扩展至 `link`, `address`, `route` 三管齐下），并在底层引入了 `"default|state UP|LOWER_UP"` 三级泛匹配。完美修补了移动蜂窝网络（Point-to-Point 协议）下发默认路由时不存在 `via` 关键词导致模块对基站重连当成瞎子的致命盲区。

## [v1.0.11] - 2026-06-12
### Fixed
- **彻底修复飞行模式和5G/4G重连盲区**：重构了事件监视器的底层内核管道匹配逻辑。先前的 `grep "default via"` 严格过滤导致模块彻底错失了移动数据网络（蜂窝默认路由为无 `via` 网关的点对点链路拓扑 `default dev rmnet`）的重连事件。新版扩大了 `ip monitor` 的监听维面（包含 `link`, `address`, `route`），并在底层引入 `"default|state UP|LOWER_UP"` 三级泛匹配以及动态延时鉴权机制（防止飞行模式开启时的假性唤醒）。现已完美实现对飞行模式秒切、Wi-Fi与5G互换的无死角秒级监控与即时阻断清理。
- 
## [v1.0.10] - 2026-06-12
### Fixed
- **彻底解决开机早期 DNS 抢跑导致的首次连接未命中问题**：Android 开机机制决定了 GmsCore 发起网络连接的速度通常快于模块框架层执行 Systemless Hosts 挂载的速度，这会导致 FCM 在开机首个长连接中必然使用非优选的默认 IP 且长期被复用。针对该生命周期痛点，现于 `service.sh` 开机 30 秒后的终极复查阶段，加入针对 `com.google.android.gms.persistent` 常驻核心进程的**无感斩断重连机制**。强迫 GMS 丢弃抢跑连接，依据已完美挂载的新 Hosts 发起二次重连，从而确保 FCM 在开机后迅速切入满血优选状态。

## [v1.0.9] - 2026-06-12
### Fixed
- **剥离双栈套接字伪装映射 (IPv4-mapped)**：修复了 Android 底层内核通过双栈 Socket 发起纯 IPv4 连接时，`ss` 输出地址被强制附加 `::ffff:` 前缀（例如 `::ffff:74.125.20.188`）的现象。该前缀会导致字符串匹配规则失效，产生“连接了优选节点却报错未命中”的假警报。现已引入正则预处理，精准脱去外壳还原真身。
- **阻断 443 端口日志洪流**：大幅优化了没有权限提取 GMS 进程名时的兜底连接抓取策略。当进程溯源失败时，脚本将自动放弃对 `443` 端口的盲扫，仅严格锁定 `5228-5230` 专属端口池。彻底杜绝了将微信、浏览器等其余国内 App 的 HTTPS 后台流量误认为 FCM 流量并疯狂刷屏的现象。

## [v1.0.8] - 2026-06-12
### Fixed
- **彻底斩断本地回环被误擦除导致的长连接断网 Bug (特大修正)**：修正了 `update_hosts` 函数覆盖改写 `/system/etc/hosts` 时没有携带本地标准回环头的逻辑漏洞。新版本改为在重写前强制在头部注入标准的 `127.0.0.1 localhost` 以及 `::1 localhost` 回环基础映射。彻底消灭了因系统丧失 Localhost 解析导致的 Box/Clash 内核代理崩溃、本地 IPC 通信死锁、进而使得 FCM 全盘断连长达数小时的灾难性技术冲突。
- **引入防死锁断流超时器**：为 `check_fcm_hosts_hit` 中的 Fake-IP 盲测机制全面封锁 `timeout 2` 超时防护。防止内置的 `nslookup` 命令在某些特定网络或未就绪的环境下遭遇服务器包丢弃而导致主脚本无限卡死、后续网络监视守护进程流产的隐患。

### Changed
- 依照用户最新需求，将安全持久化本地日志池路径由原 `fcm_fixer_safe` 规范迁移整合至 `/data/adb/fcm_fixer/`，保持结构干净洗炼。

## [v1.0.7] - 2026-06-12
### Fixed
- **解决重启后日志彻底消失的 Bug**：创立了独立的 `fcm_fixer_safe` 持久化本地日志池。脚本在向主日志写入时，会自适应向 Box 的 `/run` 目录进行增量动态镜像同步（`sync_to_box`）。彻底规避了由于 Box 模块自身初始化脚本高频擦除 `/data/adb/box/run/` 内存目录从而导致日志整体丢失、模块被误判为未启动的极高频冲突。
- **解决系统解锁/完全开机前首次连接必定受阻的隐患**：重构 `service.sh` 为**双段式多轨引导机制**。脚本启动后会立刻、无条件执行第一阶段紧急防火墙清理，剥离阻断，抢在系统解锁前完成第一波保护；随后将后续大进程放至后台等待 `sys.boot_completed=1` 以加载第二阶段的 Hosts 全局覆盖和常驻服务，实现无缝隙全开机时域覆盖。

### Added
- **网络切换连环命中追踪探测链**：针对网络切换后 FCM 需等待几秒进行重新握手连接的生理特性，新增 `track_fcm_hits_loop` 机制。在触发网络切换和防火墙清理后，会自动拉起一个每 3 秒检测一次、连续高频核对 5 次的实时探测链。现在，不论你反复切换多少次网络，日志中都将连续打出精细的五轮核对状态，百分之百高亮呈现成功命中优选 Hosts 的瞬间。

## [v1.0.6] - 2026-06-12
### Fixed
- **修复 Systemless 挂载在开机后期偶发性失效的底层 Bug**：
  1. 废除了 `common.sh` 内部更新文件时传统的 `mv -f` 指令，完全重构为 `cat >` 覆盖写入流。确保模块内 `hosts` 文件的物理 Inode 节点在运行时绝不发生改变，完美维护了 Magisk/APatch/KernelSU 早期开机时建立的 Systemless 软映射通道。
  2. 修复了刷入模块时因处于离线状态而无法创建占位符的问题。现在 `customize.sh` 会在安装结束时强制生成一个包含标准回环的 hosts 基础镜像。确保 Root 框架在早起引导阶段能无条件为本模块锁定系统级挂载点。
  3. 为 `update_hosts` 加入了主动式动态 `bind mount` 注入器，针对系统级挂载异常提供运行期强制兜底。

## [v1.0.5] - 2026-06-12
### Added
- **真·Hosts 视图反射日志机制**：重构 `hosts.log` 生成逻辑。该文件不再单纯输出模块内部生成的 Hosts 文本，而是跳过所有中间环节，直接对 Android 系统全局运行态的 `/system/etc/hosts` 执行 `cat` 抓取并反射写入日志。同时新增了系统级的 `[挂载检查]` 高亮报警器。当管理器（KernelSU / Magisk / APatch）挂载因系统分区策略失效时，日志会立即打出红色的 `[❌ ERROR]` 警报，成为判定本地挂载状态的绝对事实来源。
- **443 端口降级容灾连接追踪**：增加了对 443 端口 GMS 通信流的捕获能力。当底层专属的 5228-5230 端口遭到运营商、严格防火墙等物理环境阻断，且 FCM 自动降级激活本地 HTTPS 备用通道时，模块仍能完美追踪会话。
- **全机双向套接字穿透追踪（Fake-IP 完美克星）**：针对透明代理中因开启 Fake-IP 导致 GMS 本地连接特征变为 `198.18.x.x` 导致永远“无法匹配”的终极痛点，重构了比对树架构。新增套接字双向穿透：当盲测法判定当前处于 Fake-IP 全局接管状态时，脚本会自动从系统底层反向追踪由 Clash / Mihomo 内核二次向公网发出的直连（DIRECT）真物理 TCP 连接，并将其剥离出进行 Hosts 核对。成功实现了在 Fake-IP 严酷环境下高亮检测 Hosts 优选命中的能力。
- **分流越权智能诊断**：当在 Fake-IP 模式下发现系统触发了 FCM 请求，但代理内核并未向外直连发包时，日志会输出智能诊断提示，指导用户去检查 Clash 规则是否将 `mtalk.google.com` 误分流进了代理节点组。

### Changed
- 规范化升级了 `service.sh` 和 `action.sh` 的全线业务流，使其全部完美融入 `common.sh` 的彩色级日志框架中，全面实现开机引导、手动触发、网络切换日志体验的三元合一。
